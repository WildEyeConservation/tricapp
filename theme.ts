/**
 * App theme – default (primary) and error colors used across the app.
 */
export const theme = {
  /** App default / primary color (green) */
  primary: '#1C463C',
  /** Error / alert color (red) */
  error: '#C62828',
} as const;
